name: ejemma_wallet
description: Biafran Remittance Wallet - EJEMMA Currency and token factory
publish_to: 'none'
version: 0.2.5+8

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_riverpod: ^2.5.1
  shared_preferences: ^2.2.2
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.1
  bip39: ^1.0.6
  hex: ^0.2.0
  cupertino_icons: ^1.0.8
  url_launcher: ^6.2.6
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ejm_symbol.png
